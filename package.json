{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run typecheck && npm run build -ws --if-present",
    "build:railway": "npm run build -w artifacts/food-ordering && npm run build -w artifacts/api-server",
    "db:push": "npm run push -w lib/db",
    "start": "node artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck -w artifacts/food-ordering && npm run typecheck -w artifacts/api-server && npm run typecheck -w scripts"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "overrides": {
    "esbuild": "0.25.8",
    "lodash": "4.18.0",
    "path-to-regexp": "8.4.0",
    "picomatch": "2.3.2"
  }
}
